export function Footer() {
    return (
        <footer>
            <div className="container">
                <div>© {new Date().getFullYear()} Поток 96</div>
                <div className="muted">Контакты: potok96@local (временно)</div>
            </div>
        </footer>
    );
}
